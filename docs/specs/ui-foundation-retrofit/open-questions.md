# Open questions — ui-foundation-retrofit

None open.

The one question this file held, "Must a toast appear over an open Modal, given a z-index
cannot put it there?", moved to `ui-toast` along with the toast container on 2026-09-14. It
was decided there by the orchestrator's ruling that ratified that scope: the container is
promoted with `popover="manual"`, verify-first, with a recorded fallback (`ui-toast`
`open-questions.md`).

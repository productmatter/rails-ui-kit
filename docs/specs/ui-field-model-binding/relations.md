depends_on:
  - ui-positioning-and-navigation: Primitive E is the Field this scope extends — its id derivation, `aria-invalid`/`data-invalid` hand-off and label id are the shape `required` follows
  - ui-presentational-components: Input, Label and Textarea are the controls Field renders by default and hands a derived value and `required` to
  - ui-select: the `422` round trip this scope rebinds to a model lives on Select's docs page, and prerequisite 1 (`aria-required` on the combobox) lands under its spec
  - ui-presence-and-overlay-stack: Primitive D's `presence` module animates the help-text swap (§ Behavior, item 22); read only
  - ui-test-harness: the round-trip, accessible-name and contrast checks run in its browser lane
part_of:
  - ui-component-library: the parent-map this scope decomposes from; its rule 0 Gate 1 names this feature as its example
supersedes: []
conflicts_with: []

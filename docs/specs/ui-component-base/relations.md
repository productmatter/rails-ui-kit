depends_on:
  - ui-design-tokens: `Ui::Base`'s variant tables resolve to token-backed utility classes, so the token layer has to exist before a variant can name one
part_of:
  - ui-component-library: the parent-map this scope decomposes from
supersedes: []
conflicts_with: []
